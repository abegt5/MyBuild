import { View, Text } from "react-native-web";

export default function Onboarding() {
    return(
        <View className= "flex-1 justify-center items-center">
        <Text className="text-5xl text-primary font-bold">MyBuild</Text>
        </View>
    );
}
